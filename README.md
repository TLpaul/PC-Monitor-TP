# PC-Monitor-TP
A lightweight C# WinForms system monitor that shows real-time CPU load and RAM usage using WMI. Designed to evolve into a game-overlay-style HUD with GPU metrics and optional Arduino output for an in-case display. Built with System.Management + Timer updates; GPU load support incoming
